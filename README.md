1️⃣ What is the difference between var, let, and const?
-> var and let redeclare and reassign, const cannot redeclare and reassign.

2️⃣ What is the spread operator (...)?
-> spread operator copy same array and object value"

3️⃣ What is the difference between map(), filter(), and forEach()?
-> map() - Create new array and change value.
   filter() - Create new array match condition.
   forEach() - only works looping but return nothing.

4️⃣ What is an arrow function?
-> arrow function is short syntax function.

5️⃣ What are template literals?
-> we can write and make dynamic card/element in backticks.
